apiVersion: v1
kind: Secret
metadata: {name: pios-secrets, namespace: pios}
type: Opaque
stringData:
  PIOS_DATABASE_URL: REPLACE_FROM_SECRET_STORE
  PIOS_OIDC_ISSUER: REPLACE
  PIOS_OIDC_AUDIENCE: pios-api
  PIOS_OIDC_JWKS_URL: REPLACE
  PIOS_S3_ENDPOINT_URL: REPLACE
  PIOS_S3_ACCESS_KEY: REPLACE
  PIOS_S3_SECRET_KEY: REPLACE
